<div class="container-fluid">
  <h1>個人設定</h1>

  <% if @user.errors.any? %>
    <% @user.errors.full_messages.each do |message| %>
      <%= message %>
    <% end %>
  <% end %>

  <%= bootstrap_form_with(model: @user,
  url: {controller: :accounts, action: :update, user_id: current_user.id},
  method: :patch,
  local: true) do |f| %>

    <div class="row mb-3">
      <div class="col-12 col-md-6">
          <h3>ニックネーム</h3>
          <%= f.text_field :name, hide_label: true %>
      </div>

      <div class="col-12 col-md-6">
        <h3>アカウントの公開設定<span style="margin-right: 4em;"></span>
          <span class="fa fa-question-circle" aria-hidden="true" data-toggle="popover" title="アカウントの公開設定" data-content="<b>【公開】</b><br>
            <ul>
              <li>moku JUST NOWに公開されます。</li>
              <li>他のユーザーが成果物をみた時、「作者さん」リンクから、ニックネーム・mokuアイテム・自己紹介が見えるページにジャンプできるようになります。</li>
            </ul>
            <b>【非公開】</b><br>
            <ul>
              <li>moku JUST NOWには公開されません。</li>
              <li>他のユーザーが成果物をみた時、「作者さん」リンクが無効になります。</li>
              <li>PICK UPにも載らなくなります。</li>
            </ul>">
          </span>
        </h3>
        <%= f.select :public, options_for_public, selected: @user.public , hide_label: true%>
      </div>
    </div>

    <div class="row mb-3">
      <div class="col-12 col-md-6">
        <h3>MOKUアイテム</h3>
          作業につかう道具や機材、材料、教材などを登録しましょう！<br>
          <%= f.text_area :items, hide_label: true, size: "20x7"  %>
      </div>

      <div class="col-12 col-md-6">
        <h3>自己紹介</h3>
          自己紹介を登録しましょう！（１４０文字まで）<br>
          <%= f.text_area :introduce, hide_label: true, size: "20x7"  %>
      </div>
    </div>
    <div class="row">
      <div class="col-12 text-center">
        <%= f.submit "登録する", class: "btn btn-info btn-lg" %>
      </div>
    </div>
  <% end %>

  <%= render 'my_shared/go_mypage' %>
</div>

<%# はてなアイコン %>
<%= javascript_include_tag "application.js" %>
<script></script>