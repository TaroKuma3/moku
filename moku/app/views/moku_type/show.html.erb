<h1>MokuType#show</h1>
<h1><%= "【MOKUタグ：#{@moku_type.name}】でMOKUったもの" %></h1>
<% @mokus.each do |moku| %>
<ul>
  <li>
    <%= link_to("#{moku.created_at}のMOKU", user_moku_path(moku.user_id, moku.id)) %>
  </li>
</ul>
<% end %>

<%= render 'my_shared/go_mypage' %>